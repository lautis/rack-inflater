require "spec_helper"

RSpec.describe Rack::Inflater do
  it "has a version number" do
    expect(Rack::Inflater::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
